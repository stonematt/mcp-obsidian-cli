---
date: 2026-05-16
generated: 2026-05-16T16:50:40
branch: dev
host: m4MBPro
commits: 24
---

## 2026-05-16 - Multi-vault + full release pipeline (v1.3.0 → v1.3.2)

**Branch:** `dev` (24 commits across 3 releases)

Shipped the long-standing multi-vault + Obsidian-closed bugs as v1.3.0, then built out the full automated release pipeline — npm publish, MCP Registry publish via OIDC, GitHub Release from CHANGELOG, and auto-tag on master push. v1.3.1 partial-failed (Registry rejected description >100 chars after npm shipped); v1.3.2 catch-up release ran end-to-end clean. Tested the new auto-tag workflow live and confirmed: merge release PR → tag → npm + Registry + GitHub Release fires automatically.

**Decisions & external events**
- **MCP Registry has a 100-char `description` limit on `server.json`.** Bit us on v1.3.1: npm@1.3.1 published with provenance, then mcp-publisher rejected the catalog entry. Recovery required bumping to v1.3.2 (npm doesn't allow re-publish at same version). Saved to project memory as `reference_mcp_registry_constraints`.
- **GITHUB_TOKEN-pushed tags don't trigger workflows.** Exploited this in the auto-tag refactor: workflow pushes tag on master push AND runs publish steps inline in the same job — no double-firing, no PAT needed.
- **MCP Registry auth via OIDC** (no long-lived secret). Same pattern as npm provenance.
- **Default `cliPath` switched from `obsidian` to `obsidian-cli`** to fix duplicate-dock-icon bug on macOS. Documented in CHANGELOG migration section — existing users with `cliPath: obsidian` need to update config.
- Three live channels for v1.3.2: npm (`mcp-obsidian-cli@1.3.2`), MCP Registry (`io.github.stonematt/mcp-obsidian-cli`), GitHub Release (https://github.com/stonematt/mcp-obsidian-cli/releases/tag/v1.3.2).
- v1.3.1 sits as a "phantom" version on npm with no MCP Registry entry and no GitHub Release — harmless, npm `latest` points at 1.3.2.

**Server fixes (shipped v1.3.0)**
- `feat(server): runtime vault selection with prompt-on-unknown` — if `OBSIDIAN_VAULT` unset/unknown, server asks user to pick on first tool call; `vault=NAME` first-token override routes + remembered for session
- `fix(server): graceful startup when Obsidian closed` — no more crash on `Claude Desktop` start; pgrep-based detection (#21)
- `fix(server): respect caller vault= override; correct ENOENT binary name`
- `fix(server): prepend vault= arg instead of appending` (#23) — fixes writes landing in wrong vault when multiple loaded
- `fix(server): default cliPath to obsidian-cli, not obsidian` — kills duplicate dock icon

**Release pipeline (built across v1.3.1/v1.3.2)**
- `feat(release): publish to MCP Registry on tag` — added `server.json`, `mcp-publisher` install, OIDC auth
- `feat(release): auto-create GitHub Release from CHANGELOG on tag` — awk-extracts matching `## [vX.Y.Z]` section; falls back to `--generate-notes` if missing
- `feat(release): auto-tag on master push from package.json version` — master push trigger reads `package.json`, creates tag if missing, runs publish steps inline. Manual tag push path still works for hotfixes.
- `fix(ci): shorten server.json description to meet MCP Registry 100-char limit` (94 chars)

**Repo hygiene / docs**
- GitHub issue templates: bug report, feature request, config (#25)
- README bug-report nudge link
- CHANGELOG: retroactive 1.3.0/1.3.1 entries, then rewritten user-centric per the "plumbing stays in commit messages" rule
- `.planning/STATE.md` updated 3× to track merge events

**PRs landed**
- #22 → fix/21-graceful-no-obsidian (server crash fix bundle)
- #24 → dev: v1.3.0 release (multi-vault + Obsidian-closed)
- #25 → feat/issue-templates (docs)
- #26 → feat/mcp-registry (publish pipeline)
- #27 → dev→master release v1.3.1 (npm shipped, Registry rejected)
- #28 → dev→master release v1.3.2 (full pipeline succeeded)

**Out-of-band**
- Validated auto-tag workflow end-to-end on the v1.3.2 run — 20s total, all steps green
- Updated project memory: `reference_mcp_registry_constraints.md` indexed in MEMORY.md
