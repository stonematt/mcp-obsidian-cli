---
date: 2026-04-04
generated: 2026-04-04T08:15:00
branch: master
host: m4MBPro
commits: 9
---

## 2026-04-04 - Claude Desktop can't see prompts, ship obsidian_help instead

**Branch:** `master` (9 commits)

Discovered Claude Desktop doesn't support MCP prompts - the 1,031 lines of Obsidian knowledge from Phase 2.1 were invisible. Pivoted to exposing content via an `obsidian_help` tool, built a sniff test workflow, verified 10/10 in Claude Desktop, and shipped v1.1.0.

**obsidian_help tool**
- Added `obsidian_help({ topic: "cli|markdown|bases|canvas" })` tool that returns prompt content via the tools API
- Uses `z.enum()` for topic selection, reads from existing `promptContent` object - no new file I/O
- Kept `registerPrompt()` calls for forward compatibility when clients add prompt support

**Desktop sniff test**
- Built `test/sniff-test-prompt.md` with 10 tests covering help content (T1-T5), enriched descriptions (T6), and tool execution (T7-T10)
- Iterated on test format: added per-criterion checklists, content samples, structured reporting
- Fixed T9 context blowout by scoping `obsidian_files({ ext: "canvas" })` instead of unbounded listing
- Created `.claude/skills/desktop-sniff-test/SKILL.md` to automate the config swap + test + restore workflow

**Release 1.1.0 (PR #2)**
- Version bump, merged, published to npm
- Package: server.js + 4 prompt files (52.2 kB unpacked), no test/planning artifacts leaked
