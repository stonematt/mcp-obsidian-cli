---
date: 2026-04-08
generated: 2026-04-11T10:45:00
branch: master
host: m4MBPro
commits: 3
---

## 2026-04-08 - Fix quote corruption and slow startup

**Branch:** `master` (3 commits, PR #6)

Closed both open issues. Copilot also took a swing at #4 independently (PR #5) but the fix was already merged by then.

**Bug fixes (PR #6)**
- Fixed quote corruption in CLI args by stripping quotes in `parseArgs` regex (#3) - one-line fix in server.js
- Removed redundant `obsidian version` CLI call from startup health check (#4) - simplified `checkObsidianRunning` from 28 lines to 9, returns boolean instead of `{ running, version }`
- Rewrote health check tests to auto-detect Obsidian status via `ps aux` instead of relying on `OBSIDIAN_RUNNING` env var

**Copilot experiment**
- Opened issue-like prompt to GitHub Copilot agent, which produced PR #5 on branch `copilot/fix-slow-startup-redundant-checks`
- PR was a duplicate of the already-merged #6 fix, plus stale `package-lock.json` and `.gitignore` noise
- Closed as superseded on 2026-04-11
