---
date: 2026-04-11
generated: 2026-04-11T10:45:00
branch: master
host: m4MBPro
commits: 9
---

## 2026-04-11 - Production readiness and v1.2.0 release

**Branch:** `master` (9 commits)

Full production readiness push: landed the array-based args refactor, added CI, unit tests, changelog, and shipped v1.2.0. Sniff test 11/11 in Claude Desktop. Also code-reviewed and closed the stale Copilot PR #5.

**Array-based args refactor**
- Switched all 12 convenience tools from string-based command building (`cmd += ' file="${file}"'`) to array-based (`args.push('file=${file}')`)
- Bypasses `parseArgs` entirely for typed tools, eliminating quote corruption at the source rather than patching around it
- `run()` and `runTool()` now accept string or array input; `parseArgs` preserved for the generic `obsidian` passthrough tool

**Testability and unit tests**
- Extracted `parseArgs`, `loadConfig`, `text`, `errorResult` to `lib/helpers.js` - pure functions importable without triggering server startup
- Added 12 unit tests (node:test): parseArgs (6), loadConfig (4), MCP response helpers (2)
- Parameterized `loadConfig(configFile)` for test isolation with temp YAML files

**CI/CD**
- Added `.github/workflows/test.yml` - runs on push/PR to master, Node 18/20/22 matrix
- Added `.github/workflows/publish.yml` - npm publish with provenance on `v*` tags
- Configured `NPM_TOKEN` repo secret for automated publishing

**Release v1.2.0**
- Added CHANGELOG.md with retroactive entries for 1.0.0 through 1.2.0
- Version bump, tagged `v1.2.0`, publish workflow ran green on first try
- Sniff test 11/11 passed (new T11 tests create-in-subdirectory with rich content, wikilinks, quoted strings)
- Fixed T11 cleanup command in sniff-test-prompt.md (`trash` -> `delete`)

**Repo housekeeping**
- Code-reviewed Copilot's PR #5, posted review comment, closed as superseded
- Added `.claude/settings.json` with scoped git/npm/gh permissions (PR #7)
- Updated sniff test skill to delegate evaluation to Sonnet agent
- Promoted journal `ls` permission to global `~/.claude/settings.json`
- Switched Claude Desktop config from local `server.js` to `npx mcp-obsidian-cli@latest`
